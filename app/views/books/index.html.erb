  <h1>Books</h1>



  <table>
  <tr>
    <th>Title</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  <% @books.each do |book| %>
  <tr>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><p class="botton"><%=link_to "show", book_path(book) %><p></td>
    <td><p class="botton"><%=link_to "Edit", edit_book_path(book) %><p></td>
    <td><p class="botton"><%=link_to "Destroy", book_path(book),method: :'delete' %><p></td>
  </tr>
  <% end %>

</table>




<!-- table -->


  <h4>Title</h4>
  <%= form_for @book do |f| %>
  <%= f.text_field :title %>
  <br>
   <h4>Body</h4>
  <%= f.text_area :body %>
  <p><%= f.submit 'Crete Book' %></p>
<% end %>